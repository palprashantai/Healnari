const fs = require('fs'); const files = ['Appointments.jsx', 'Prescriptions.jsx', 'Reports.jsx', 'Staff.jsx', 'Telemedicine.jsx']; files.forEach(f => { const path = 'src/doctor/pages/' + f; let c = fs.readFileSync(path, 'utf8'); c = c.replace(/className=\"absolute right-0 top-full/g, 'className=\"absolute left-0 sm:left-auto sm:right-0 top-full'); fs.writeFileSync(path, c); console.log('Fixed', f); });
